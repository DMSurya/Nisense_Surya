# Config persistence feature overlay
# Persists struct app_config via Zephyr settings (NVS key cm/app).
# Does not require APP_FEATURE_FILE_SYSTEM.
#
# Applied via EXTRA_CONF_FILE when enabling APP_FEATURE_CONFIG_PERSIST.
# SETTINGS + SETTINGS_NVS are already enabled in conf/core/storage.conf.
